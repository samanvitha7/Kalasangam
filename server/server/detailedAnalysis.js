const mongoose = require('mongoose');
const User = require('./models/User');
require('dotenv').config();

const detailedAnalysis = async () => {
  try {
    await mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/kalasangam');
    console.log('🔍 Starting Detailed Security Analysis');
    
    const db = mongoose.connection.db;
    
    // 1. Investigate artistprofiles collection (mentioned as compromised)
    console.log('\n=== INVESTIGATING artistprofiles COLLECTION ===');
    const artistProfilesCollection = db.collection('artistprofiles');
    const artistProfiles = await artistProfilesCollection.find({}).toArray();
    
    console.log(`Total artist profiles: ${artistProfiles.length}`);
    
    // Look for recently created/modified profiles
    const recentProfiles = artistProfiles.filter(profile => {
      const created = new Date(profile.createdAt || profile._id.getTimestamp());
      const daysDiff = (new Date() - created) / (1000 * 60 * 60 * 24);
      return daysDiff < 7;
    });
    
    if (recentProfiles.length > 0) {
      console.log(`\n⚠️  RECENT ARTIST PROFILES (Last 7 days):`);\n      recentProfiles.forEach((profile, index) => {
        console.log(`${index + 1}. Name: ${profile.name || 'No name'}`);\n        console.log(`   Email: ${profile.email || 'No email'}`);\n        console.log(`   Created: ${profile.createdAt || profile._id.getTimestamp()}`);\n        console.log(`   Profile ID: ${profile._id}`);\n        \n        // Check for suspicious patterns\n        if (profile.email && profile.email.includes('.ru')) {\n          console.log(`   🚨 RUSSIAN EMAIL DOMAIN!`);\n        }\n        if (profile.name && (profile.name.toLowerCase().includes('admin') || profile.name.toLowerCase().includes('hack'))) {\n          console.log(`   🚨 SUSPICIOUS NAME!`);\n        }\n        console.log('');\n      });\n    }\n    \n    // 2. Check authentication system integrity\n    console.log('\n=== AUTHENTICATION SYSTEM ANALYSIS ===');\n    \n    // Look for any users with password reset tokens\n    const usersWithTokens = await User.find({\n      $or: [\n        { resetPasswordToken: { $ne: null } },\n        { emailVerificationToken: { $ne: null } }\n      ]\n    });\n    \n    if (usersWithTokens.length > 0) {\n      console.log(`⚠️  USERS WITH ACTIVE TOKENS:`);\n      usersWithTokens.forEach((user, index) => {\n        console.log(`${index + 1}. ${user.name} (${user.email})`);\n        if (user.resetPasswordToken) console.log(`   - Has password reset token`);\n        if (user.emailVerificationToken) console.log(`   - Has email verification token`);\n      });\n    }\n    \n    // 3. Check for any admin accounts that can't log in\n    console.log('\\n=== ADMIN LOGIN STATUS CHECK ===');\n    const allAdmins = await User.find({ role: 'Admin' });\n    \n    for (const admin of allAdmins) {\n      console.log(`\\nAdmin: ${admin.name} (${admin.email})`);\n      console.log(`  Created: ${admin.createdAt}`);\n      console.log(`  Email Verified: ${admin.isEmailVerified}`);\n      console.log(`  Has Password: ${admin.password ? 'Yes' : 'No'}`);\n      console.log(`  Google ID: ${admin.googleId ? 'Yes' : 'No'}`);\n      \n      // Check if account has required login credentials\n      if (!admin.password && !admin.googleId) {\n        console.log(`  🚨 NO LOGIN METHOD AVAILABLE!`);\n      }\n      \n      if (admin.resetPasswordToken) {\n        console.log(`  ⚠️  Password reset token active`);\n        console.log(`  ⚠️  Token expires: ${admin.resetPasswordExpire}`);\n      }\n    }\n    \n    // 4. Check for any suspicious database modifications\n    console.log('\\n=== CHECKING FOR SUSPICIOUS MODIFICATIONS ===');\n    \n    // Check users collection for recently modified accounts\n    const recentlyModified = await User.find({\n      updatedAt: { $gte: new Date(Date.now() - 24 * 60 * 60 * 1000) } // Last 24 hours\n    });\n    \n    if (recentlyModified.length > 0) {\n      console.log(`⚠️  USERS MODIFIED IN LAST 24 HOURS:`);\n      recentlyModified.forEach((user, index) => {\n        console.log(`${index + 1}. ${user.name} (${user.role})`);\n        console.log(`   Email: ${user.email}`);\n        console.log(`   Modified: ${user.updatedAt}`);\n        console.log(`   Created: ${user.createdAt}`);\n        \n        // Check if account was modified very soon after creation\n        const timeDiff = new Date(user.updatedAt) - new Date(user.createdAt);\n        if (timeDiff < 60000) { // Modified within 1 minute of creation\n          console.log(`   🚨 MODIFIED IMMEDIATELY AFTER CREATION!`);\n        }\n        console.log('');\n      });\n    }\n    \n    // 5. Look for any accounts with admin privileges but suspicious creation patterns\n    const suspiciousAdmins = await User.find({\n      role: 'Admin',\n      $or: [\n        { name: /admin/i },\n        { name: /root/i },\n        { name: /hack/i },\n        { email: /.ru$/ },\n        { email: /temp/i },\n        { email: /test/i }\n      ]\n    });\n    \n    if (suspiciousAdmins.length > 0) {\n      console.log('\\n🚨 SUSPICIOUS ADMIN ACCOUNTS:');\n      suspiciousAdmins.forEach((admin, index) => {\n        console.log(`${index + 1}. ${admin.name} (${admin.email})`);\n        console.log(`   Created: ${admin.createdAt}`);\n        console.log(`   ID: ${admin._id}`);\n        \n        if (admin.email && admin.email.includes('.ru')) {\n          console.log(`   🚨 RUSSIAN DOMAIN DETECTED!`);\n        }\n      });\n    }\n    \n    console.log('\\n🔍 Detailed Analysis Complete');\n    \n    // 6. Generate security report\n    console.log('\\n=== SECURITY BREACH SUMMARY ===');\n    const securityIssues = [];\n    \n    if (allAdmins.some(admin => !admin.password && !admin.googleId)) {\n      securityIssues.push('Admin accounts without login credentials detected');\n    }\n    \n    if (allAdmins.some(admin => admin.name.toLowerCase().includes('admin'))) {\n      securityIssues.push('Generic admin account names detected');\n    }\n    \n    if (recentlyModified.length > 0) {\n      securityIssues.push(`${recentlyModified.length} user accounts modified recently`);\n    }\n    \n    if (usersWithTokens.length > 0) {\n      securityIssues.push(`${usersWithTokens.length} accounts with active tokens`);\n    }\n    \n    if (securityIssues.length > 0) {\n      console.log('\\n🚨 SECURITY ISSUES FOUND:');\n      securityIssues.forEach((issue, index) => {\n        console.log(`${index + 1}. ${issue}`);\n      });\n    } else {\n      console.log('\\n✅ No immediate security threats detected');\n    }\n    \n    process.exit(0);\n    \n  } catch (error) {\n    console.error('❌ Error during detailed analysis:', error.message);\n    process.exit(1);\n  }\n};\n\ndetailedAnalysis();
